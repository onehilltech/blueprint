#!/usr/bin/env node
'use strict';

var program = require ('commander');

program
  .command ('setup', 'run me first')
  .command ('client', 'manage client access to resources')
  .command ('account', 'manage user access to resources')
  .parse (process.argv);
