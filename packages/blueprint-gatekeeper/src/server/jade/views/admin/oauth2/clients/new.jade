extends ../../../../layouts/authenticated

block content
    div.container
       h2 New Client Registration

       p
          | Complete the form below to register a new client with Gatekeeper. The client will be
          | given a registration id that must be used to uniquely identify the client. If the
          | registration id is lost, then the client must be registered again.


       div.alert.alert-info(role="alert")
          strong Client id
          |  and
          strong  client secret
          |  are auto-generated by the system.

       p
          form.form-horizontal(method="post", action="/admin/oauth2/clients/new")
             div.form-group(class=errors && errors.name ? "has-error has-feedback" : "")
                label.col-md-2.control-label(for="name") Name

                div.col-md-10
                   input#name.form-control(type="text", name="name", value=(input && input.name ? input.name : "") placeholder="Enter client name")
                   - if (errors && errors.name)
                      span.glyphicon.glyphicon-remove.form-control-feedback(aria-hidden="true")

             div.form-group(class=errors && errors.email ? "has-error has-feedback" : "")
                label.col-md-2.control-label(for="email") Contact address

                div.col-md-10
                   input#email.form-control(type="email", name="email", value=(input && input.email ? input.email : "") placeholder="Enter email address")
                   - if (errors && errors.email)
                      span.glyphicon.glyphicon-remove.form-control-feedback(aria-hidden="true")

             div.form-group(class=errors && errors.redirect_uri ? "has-error has-feedback" : "")
                label.col-md-2.control-label(for="redirect_uri") Redirect URL

                div.col-md-10
                   input#redirect_uri.form-control(type="text", name="redirect_uri", value=(input && input.redirect_uri ? input.redirect_uri : "") placeholder="Enter redirect URI")
                   - if (errors && errors.redirect_uri)
                      span.glyphicon.glyphicon-remove.form-control-feedback(aria-hidden="true")

             div.form-group
                div.col-md-offset-2.col-md-10
                   button.btn.btn-default(type="submit") Register client
