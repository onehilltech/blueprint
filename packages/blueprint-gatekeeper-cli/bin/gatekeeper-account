#!/usr/bin/env node

const program = require ('commander');

program
  .command ('add', 'add a new user account')
  .command ('remove', 'remove an existing user account')
  .command ('passwd', 'change the password for an account')
  .parse (process.argv);
