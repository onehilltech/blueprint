{
  "name": "@onehilltech/blueprint-mongodb",
  "description": "Blueprint.js module for MongoDB",
  "version": "0.36.0",
  "private": false,
  "main": "./lib",
  "keywords": [
    "blueprint",
    "blueprint-module",
    "mongodb",
    "mongoose"
  ],
  "scripts": {
    "test": "env NODE_ENV=test mocha --recursive tests/unit-tests",
    "cover": "env NODE_ENV=test istanbul cover _mocha --recursive tests/unit-tests",
    "coveralls": "npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/onehilltech/blueprint-mongodb.git"
  },
  "dependencies": {
    "@onehilltech/blueprint": "^4.0.0-6",
    "@onehilltech/dab": "^0.10.3",
    "moment": "^2.19.1"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "istanbul": "^0.4.5",
    "mocha": "^4.0.1",
    "superagent": "^3.6.3",
    "supertest": "^3.0.0"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=6.0.0"
  }
}
