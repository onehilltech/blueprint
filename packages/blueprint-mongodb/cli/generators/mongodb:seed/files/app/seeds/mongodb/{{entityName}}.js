const { Seed } = require ('@onehilltech/blueprint-mongodb');
const dab = require ('@onehilltech/dab');

/**
 * @class
 *
 * The implementation of the seed definition. The seed definition must implement the
 * model() function. It must return a dab definition.
 */
module.exports = new Seed ({
  model () {
    return {

    }
  }
});
