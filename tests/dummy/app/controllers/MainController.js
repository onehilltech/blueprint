const Blueprint = require ('../../../../lib');

module.exports = Blueprint.Controller.extend ({
  __invoke () {
    return (req, res) => {

    };
  },

  performGet () {
    return (res, res) => {

    };
  }
});
