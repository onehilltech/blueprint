const {
  Controller,
  Action
} = require ('../../../../lib');

module.exports = Controller.extend ({
  __invoke () {
    return Action.extend ({

    });
  },

  performGet () {
    return Action.extend ({

    });
  }
});
